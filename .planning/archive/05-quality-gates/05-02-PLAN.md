---
phase: 05-quality-gates
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .claude/commands/agency/review.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [QA-01, QA-05]
user_setup: []

must_haves:
  truths:
    - "/agency:review loads the review-loop skill and follows its full process"
    - "The command parses $ARGUMENTS for --phase flag or auto-detects current phase from STATE.md"
    - "Review agent selection follows review-loop Section 2 with user confirmation before spawning"
    - "The review → fix → re-review loop follows review-loop Sections 4-6 with max 3 cycles"
    - "Phase is marked complete ONLY after review passes (review-loop Section 7)"
    - "Escalation follows review-loop Section 8 when 3 cycles exhaust without resolution"
    - "The command routes to /agency:plan N+1 on successful review pass"
  artifacts:
    - path: ".claude/commands/agency/review.md"
      provides: "Fully functional quality review command with dev-QA loop"
      min_lines: 100
      contains: "review-loop"
  key_links:
    - from: ".claude/commands/agency/review.md"
      to: ".claude/skills/agency/review-loop.md"
      via: "execution_context reference"
      pattern: "@./.claude/skills/agency/review-loop.md"
    - from: ".claude/commands/agency/review.md"
      to: ".claude/skills/agency/execution-tracker.md"
      via: "execution_context reference for phase completion tracking"
      pattern: "@./.claude/skills/agency/execution-tracker.md"
    - from: ".claude/commands/agency/review.md"
      to: ".planning/STATE.md"
      via: "context reference for current phase detection and state updates"
      pattern: "STATE.md"
---

<objective>
Update `/agency:review` from its Phase 1 scaffold into a fully functional command that orchestrates the dev-QA review cycle with personality-injected testing agents, structured feedback, fix routing, and escalation.

Purpose: Make `/agency:review` the complete quality gate — a user types the command and gets a rigorous, iterative review of their phase output.
Output: Updated `.claude/commands/agency/review.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-quality-gates/05-CONTEXT.md
@.planning/phases/05-quality-gates/05-01-SUMMARY.md

Relevant source files:
@.claude/skills/agency/workflow-common.md
@.claude/skills/agency/agent-registry.md
@.claude/skills/agency/review-loop.md
@.claude/skills/agency/execution-tracker.md
@.claude/commands/agency/review.md
@.claude/commands/agency/build.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /agency:review command to full implementation</name>
  <files>.claude/commands/agency/review.md</files>
  <action>
Replace the scaffold content in `.claude/commands/agency/review.md` with a fully functional command. Keep the existing frontmatter fields but update as needed.

The updated command should have:

**Frontmatter** (update from scaffold):
```yaml
---
name: agency:review
description: Run quality review cycle with testing/QA agents
argument-hint: "[--phase N]"
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent, TeamCreate, TeamDelete, TaskCreate, TaskUpdate, TaskList, SendMessage, AskUserQuestion]
---
```

Note: The `allowed-tools` list includes Agent, TeamCreate, Task tools (for spawning review and fix agents), and AskUserQuestion (for reviewer confirmation and escalation decisions).

**execution_context** — Load all required skills (uncomment review-loop, add execution-tracker):
```
@./.claude/skills/agency/workflow-common.md
@./.claude/skills/agency/agent-registry.md
@./.claude/skills/agency/review-loop.md
@./.claude/skills/agency/execution-tracker.md
```

**context** — Reference state files:
```
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
```

**process** — Fully specified steps (not placeholders):

```
1. DETERMINE TARGET PHASE
   - Check $ARGUMENTS for --phase N flag (e.g., `/agency:review --phase 4`)
   - If no flag: read STATE.md to determine current phase
     - Use the phase number from "Phase: N of M" in Current Position
     - Valid states for review: "executed, pending review" or "partial"
     - If status says "planned" or "pending": error — "Phase {N} hasn't been executed yet. Run /agency:build first."
     - If status says "complete": error — "Phase {N} already passed review. Run /agency:plan {N+1} for the next phase."
   - Validate: phase must exist in ROADMAP.md
   - Check that phase directory has SUMMARY.md files (proof of execution):
     - If no SUMMARY.md files: error — "Phase {N} has no execution summaries. Run /agency:build first."
   - Display: "Reviewing Phase {N}: {phase_name}"

2. GATHER PHASE CONTEXT
   Follow review-loop skill Section 3, Step 1 (Gather phase artifacts):
   - Read the phase CONTEXT.md for the phase goal and success criteria
   - Read all PLAN.md files in the phase directory to understand what should have been built
   - Read all SUMMARY.md files to understand what was actually done
   - From each plan's files_modified, build the complete list of files to review
   - Read the ROADMAP.md success criteria for this phase
   - Display context summary:
     "Phase {N}: {phase_name}
      Goal: {phase_goal}
      Plans executed: {count} ({list names and statuses})
      Files to review: {count}
      {file list}"

3. SELECT REVIEW AGENTS
   Follow review-loop skill Section 2 (Review Agent Selection):
   a. Classify the phase type from its CONTEXT.md and SUMMARY.md content
      (code, api, design, marketing, infrastructure, workflow)
   b. Map phase type to primary + secondary review agents using the selection table
   c. Present selected reviewers to user via AskUserQuestion:
      "Review agents for Phase {N}: {phase_name}?"
      Options:
      - "{primary_agent_name} + {secondary_agent_name}" (Recommended)
      - "{primary_agent_name} only" — single reviewer, faster but less thorough
      - "{alternative_agent_name} + {primary_agent_name}" — different reviewer pair
   d. If user selects "Other": accept custom agent IDs from user input
   e. Validate selected agents exist in agent-registry

4. EXECUTE REVIEW CYCLE
   Initialize: cycle_count = 0

   **Team Setup** (once, before review loop):
   - Call TeamCreate with team_name: "phase-{NN}-review" (e.g., "phase-05-review")

   **LOOP START** (max 3 iterations):

   a. Increment cycle_count
      Announce: "Review cycle {cycle_count}/3"

   b. Spawn review agents (review-loop Section 3):
      For each selected reviewer:
      - Read the reviewer's personality .md file (full content)
      - Read all files in the review file list
      - Construct the review prompt per review-loop Section 3, Step 3:
        personality + phase context + success criteria + review instructions + feedback format
      - Spawn via Agent tool:
        - subagent_type: "general-purpose"
        - model: "sonnet"
        - name: "{agent-id}-review-{NN}-c{cycle}" (e.g., "testing-qa-verification-specialist-review-05-c1")
        - team_name: "phase-{NN}-review"
      - Spawn ALL reviewers in parallel (multiple Agent calls in one message)

   c. Collect review results via SendMessage (review-loop Section 4):
      - Wait for all review agents to send findings
      - Parse findings into structured format: file, line/section, severity, issue, details, fix
      - Deduplicate across reviewers (highest severity wins on conflicts)
      - Triage: must-fix (BLOCKER + WARNING) vs nice-to-have (SUGGESTION)

   d. Display findings table to user (review-loop Section 4, Step 4):
      ## Review Findings — Cycle {cycle_count}/3
      | # | Severity | File | Issue | Reviewer |
      ...
      Blockers: {count} | Warnings: {count} | Suggestions: {count}
      Verdict: {aggregate verdict}

   e. If verdict is PASS (no blockers, no warnings):
      - Break the loop — go to step 5
   f. If verdict is NEEDS WORK or FAIL AND cycle_count < 3:
      - Route fixes per review-loop Section 5 (Fix Cycle):
        - Determine fix agent for each finding (agent-registry or autonomous)
        - Group findings by fix agent
        - Construct fix prompts per review-loop Section 5, Step 2
        - Spawn fix agents in parallel
        - Wait for fix results via SendMessage
        - Create fix commit:
          fix(agency): review cycle {C} fixes for phase {N}
        - Go back to LOOP START for re-review
          (re-review scopes to modified files per review-loop Section 6)
   g. If cycle_count >= 3 AND blockers remain:
      - Break the loop — go to step 5 (escalation path)

   **LOOP END**

   **Team Teardown** (always, after loop ends):
   - Send shutdown_request to all agents via SendMessage
   - Wait for shutdown confirmations
   - Call TeamDelete to clean up

5. COMPLETE REVIEW
   Determine outcome:

   **Path A: Review Passed** (review-loop Section 7)
   - Generate .planning/phases/{NN}-{slug}/{NN}-REVIEW.md with review summary
   - Mark phase complete in STATE.md and ROADMAP.md:
     - STATE.md Phase: {N} of {total} (complete)
     - STATE.md Status: "Phase {N} complete — review passed ({cycles} cycle(s))"
     - STATE.md Next Action: "Run `/agency:plan {N+1}` for the next phase"
     - ROADMAP.md: check [x], Status = "Complete"
   - Update REQUIREMENTS.md: mark QA requirements as complete
   - Create review completion commit:
     chore(agency): phase {N} review passed — {phase_name}
   - Display:
     "Phase {N}: {phase_name} — Review PASSED ({cycles} cycle(s))
      Run `/agency:plan {N+1}` for the next phase."

   **Path B: Review Escalated** (review-loop Section 8)
   - Generate .planning/phases/{NN}-{slug}/{NN}-REVIEW.md with escalation report
   - Update STATE.md:
     - Status: "Phase {N} review escalated — {count} unresolved blocker(s)"
     - Next Action: "Review {NN}-REVIEW.md and decide next steps"
   - Display escalation table with remaining blockers
   - Use AskUserQuestion: "How would you like to proceed?"
     Options:
     - "Fix manually and re-run /agency:review" — exit, let user work
     - "Accept as-is and move to /agency:plan {N+1}" — mark phase complete despite issues
     - "Investigate further" — exit for user to diagnose
   - If user accepts as-is:
     - Mark phase complete (same as Path A)
     - Note in REVIEW.md: "Accepted with {count} unresolved blocker(s) by user decision"

6. ROUTE TO NEXT ACTION
   - If review passed or user accepted:
     "Phase {N}: {phase_name} complete.
      Next: Run `/agency:plan {N+1}` to plan the next phase."
     OR if this was the last phase:
     "All phases complete! The Agency Workflows project is finished."
   - If escalated and user chose to fix manually:
     "Fix the issues listed in .planning/phases/{NN}-{slug}/{NN}-REVIEW.md
      Then re-run `/agency:review` to verify."
   - Do NOT automatically trigger /agency:plan — let the user decide when.
```

IMPORTANT:
- The process steps must be detailed enough that Claude can execute them without ambiguity
- Each step references the specific review-loop skill section it follows
- The command handles all three outcomes: pass, escalation, and user override
- Review agent selection includes user confirmation via AskUserQuestion
- The fix loop is bounded (max 3 cycles) with clear escalation
- Team cleanup happens on ALL paths (success, failure, escalation)
  </action>
  <verify>
Verify the command file is updated:
```bash
grep "review-loop" .claude/commands/agency/review.md
```
Should match the execution_context reference.

Verify execution-tracker is referenced:
```bash
grep "execution-tracker" .claude/commands/agency/review.md
```
Should match.

Verify it's no longer a scaffold:
```bash
grep -c "Phase 5" .claude/commands/agency/review.md
```
Should return 0 (no more "Phase 5 will implement this" placeholders).

Verify Agent tool is in allowed-tools:
```bash
grep "Agent" .claude/commands/agency/review.md
```
Should match.

Verify AskUserQuestion is in allowed-tools:
```bash
grep "AskUserQuestion" .claude/commands/agency/review.md
```
Should match.

Verify it has at least 6 process steps:
```bash
grep -c "^[0-9]\+\." .claude/commands/agency/review.md
```
Should return 6.

Verify it documents the 3-cycle loop:
```bash
grep -c "cycle\|3 iteration\|max 3" .claude/commands/agency/review.md
```
Should return 5+ matches.

Verify escalation path:
```bash
grep -c "escalat\|ESCALAT" .claude/commands/agency/review.md
```
Should return 3+ matches.

Verify team teardown:
```bash
grep "TeamDelete" .claude/commands/agency/review.md
```
Should match.
  </verify>
  <done>/agency:review command is fully functional with 6-step process: determine phase, gather context, select reviewers (with confirmation), execute review cycle (3-cycle max with fix routing), complete review (pass or escalation), route to next action.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to mark QA requirements complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update `.planning/REQUIREMENTS.md` to mark QA-01 through QA-05 as complete (change `- [ ]` to `- [x]`).

Only update the QA requirements — do not modify any other sections.
  </action>
  <verify>
```bash
grep -c "\- \[x\] QA" .planning/REQUIREMENTS.md
```
Should return 5.
  </verify>
  <done>QA-01 through QA-05 marked as complete in REQUIREMENTS.md.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.claude/commands/agency/review.md` is no longer a scaffold — has full 6-step process
- [ ] Command references all 4 skills: workflow-common, agent-registry, review-loop, execution-tracker
- [ ] Command references state files in context section (PROJECT, ROADMAP, STATE)
- [ ] Process includes phase detection with --phase flag and auto-detect fallback
- [ ] Process validates phase has been executed (SUMMARY.md files exist)
- [ ] Process includes review agent selection with user confirmation via AskUserQuestion
- [ ] Process follows review-loop sections for structured feedback and fix routing
- [ ] Process enforces 3-cycle maximum with escalation
- [ ] Process handles all three outcomes: pass, escalation, user override
- [ ] Phase marked complete ONLY after review passes (or user explicitly accepts)
- [ ] Team setup and teardown happen on all paths
- [ ] Agent, TeamCreate, and AskUserQuestion are in allowed-tools
- [ ] Routes to /agency:plan N+1 on completion
- [ ] QA requirements marked complete in REQUIREMENTS.md
- [ ] No placeholder references to "Phase 5" remain in review.md
</verification>

<success_criteria>
- /agency:review is a complete, executable command (not a scaffold)
- The 6-step process covers the full flow from phase detection to completion routing
- Skills are properly wired via execution_context references
- Review agents are personality-injected and spawned in parallel
- Feedback is structured with file references and severity levels
- Fix routing uses agent-registry to match issues to appropriate fix agents
- 3-cycle cap prevents infinite loops with clear escalation path
- User has confirmation gates for agent selection and escalation decisions
- Phase completion is gated on review pass
- Requirements tracking is updated
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-gates/05-02-SUMMARY.md`

Then update `.planning/STATE.md`:
- Phase: 5 of 6 (planned)
- Status: Phase 5 planned — 2 plans across 2 waves
- Last Activity: Phase 5 planning (2026-03-01)
- Next Action: Run `/agency:build` to execute Phase 5: Quality Gates

And update `.planning/ROADMAP.md` progress table:
- Phase 5: Plans = 2, Completed = 0, Status = Planned
</output>
