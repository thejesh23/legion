---
phase: 04-parallel-execution
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - .claude/commands/agency/build.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [EXEC-01]
user_setup: []

must_haves:
  truths:
    - "/agency:build loads the wave-executor and execution-tracker skills and follows their full process"
    - "The command parses $ARGUMENTS for --phase flag or auto-detects current phase from STATE.md"
    - "Plan discovery reads the phase directory and validates plan structure before execution"
    - "Wave execution follows wave-executor skill: parallel Agent calls within waves, sequential between waves"
    - "Progress tracking follows execution-tracker skill: STATE.md after each plan, ROADMAP.md after each wave, git commit per plan"
    - "The command routes to /agency:review on completion"
  artifacts:
    - path: ".claude/commands/agency/build.md"
      provides: "Fully functional parallel execution command"
      min_lines: 80
      contains: "wave-executor"
  key_links:
    - from: ".claude/commands/agency/build.md"
      to: ".claude/skills/agency/wave-executor.md"
      via: "execution_context reference"
      pattern: "@./.claude/skills/agency/wave-executor.md"
    - from: ".claude/commands/agency/build.md"
      to: ".claude/skills/agency/execution-tracker.md"
      via: "execution_context reference"
      pattern: "@./.claude/skills/agency/execution-tracker.md"
    - from: ".claude/commands/agency/build.md"
      to: ".planning/STATE.md"
      via: "context reference for current phase detection"
      pattern: "STATE.md"
---

<objective>
Update `/agency:build` from its Phase 1 scaffold into a fully functional command that orchestrates parallel plan execution with personality-injected agents, progress tracking, and atomic commits.

Purpose: Make `/agency:build` the complete execution entry point — a user types the command and watches agents execute plans in parallel with full progress tracking.
Output: Updated `.claude/commands/agency/build.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-parallel-execution/04-CONTEXT.md
@.planning/phases/04-parallel-execution/04-01-SUMMARY.md
@.planning/phases/04-parallel-execution/04-02-SUMMARY.md

Relevant source files:
@.claude/skills/agency/workflow-common.md
@.claude/skills/agency/agent-registry.md
@.claude/skills/agency/wave-executor.md
@.claude/skills/agency/execution-tracker.md
@.claude/commands/agency/build.md
@.claude/commands/agency/plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /agency:build command to full implementation</name>
  <files>.claude/commands/agency/build.md</files>
  <action>
Replace the scaffold content in `.claude/commands/agency/build.md` with a fully functional command. Keep the existing frontmatter fields but update as needed.

The updated command should have:

**Frontmatter** (update from scaffold):
```yaml
---
name: agency:build
description: Execute current phase plans with parallel agent teams
argument-hint: "[--phase N]"
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent, TeamCreate, TaskCreate, TaskUpdate, TaskList, SendMessage]
---
```

Note: The `allowed-tools` list includes Agent, TeamCreate, and Task tools because this command spawns agents and coordinates via Teams.

**execution_context** — Load all required skills (uncomment wave-executor, add execution-tracker):
```
@./.claude/skills/agency/workflow-common.md
@./.claude/skills/agency/agent-registry.md
@./.claude/skills/agency/wave-executor.md
@./.claude/skills/agency/execution-tracker.md
```

**context** — Reference state files:
```
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
```

**process** — Fully specified steps (not placeholders):

```
1. DETERMINE TARGET PHASE
   - Check $ARGUMENTS for --phase N flag
   - If no flag: read STATE.md to determine current phase
     - Use the phase number from "Phase: N of M" in Current Position
     - If status says "planned" or "pending": that's the target
     - If status says "executed" or "complete": error — "Phase {N} already executed. Run /agency:plan {N+1} for next phase."
   - Validate: phase must exist in ROADMAP.md and have plan files
   - If no plans exist: error — "No plans found for Phase {N}. Run /agency:plan {N} first."

2. DISCOVER PLANS
   Follow wave-executor skill Section 2 (Plan Discovery):
   - Read the phase directory: .planning/phases/{NN}-{slug}/
   - Find all {NN}-{PP}-PLAN.md files
   - Parse YAML frontmatter from each plan
   - Build the wave map: group plans by wave number
   - Validate: no circular dependencies, all depends_on references valid
   - Display plan summary to user:
     "Phase {N}: {name} — {plan_count} plans across {wave_count} waves"
     For each wave: list plan numbers and names

3. PRE-EXECUTION CONFIRMATION
   - Show the user what's about to execute:
     - Phase name and number
     - Number of plans and waves
     - Agents that will be spawned (from plan frontmatter)
     - Files that will be modified (aggregated from all plans)
   - Use AskUserQuestion: "Ready to execute Phase {N}?"
     - "Execute all plans" — proceed
     - "Execute specific wave only" — ask which wave, execute just that wave
     - "Cancel" — abort, no changes made

4. EXECUTE WAVES
   Follow wave-executor skill Section 4 (Wave Execution):
   For each wave (sequential):
   a. Announce: "Starting Wave {W} — {plan_count} plan(s)"
   b. For each plan in the wave:
      - Follow wave-executor Section 3 (Personality Injection)
      - Read agent personality .md file (if not autonomous)
      - Construct the agent prompt with personality + plan
      - Spawn agent via Agent tool with model: "sonnet"
   c. Spawn all wave agents in parallel (multiple Agent calls in one message)
   d. Wait for all agents to complete
   e. Process results: follow wave-executor Section 5 (Agent Result Processing)
      - Write summary files for each completed plan
   f. Track progress: follow execution-tracker Section 2 (Plan Completion)
      - Update STATE.md for each plan
      - Create atomic git commit for each successful plan
   g. Track wave: follow execution-tracker Section 3 (Wave Completion)
      - Update ROADMAP.md progress table
      - Report wave status
   h. If ANY plan in the wave failed: STOP
      - Do NOT proceed to next wave
      - Report failures with summary references
      - Suggest: "Run /agency:review to diagnose failures"

5. COMPLETE PHASE EXECUTION
   Follow execution-tracker Section 4 (Phase Completion):
   - Calculate final phase status (all passed vs. some failed)
   - Update STATE.md with phase completion status
   - Update ROADMAP.md progress table
   - Display final progress with plan-by-plan results table

6. ROUTE TO NEXT ACTION
   - If all plans passed:
     "Phase {N}: {name} complete. Run `/agency:review` to verify the work."
   - If some plans failed:
     "Phase {N}: {name} partial — {count} plan(s) failed.
      Review summaries: .planning/phases/{NN}-{slug}/{NN}-{PP}-SUMMARY.md
      Then run `/agency:review` for diagnosis."
   - Do NOT automatically trigger /agency:review — let the user decide when
```

IMPORTANT:
- The process steps must be detailed enough that Claude can execute them without ambiguity
- Each step references the specific skill section it follows
- The command handles both the happy path (all plans pass) and failure path (some plans fail)
- Pre-execution confirmation prevents accidental execution
- Wave-by-wave progress is visible to the user throughout
- AskUserQuestion is used for the execution confirmation gate
  </action>
  <verify>
Verify the command file is updated:
```bash
grep "wave-executor" .claude/commands/agency/build.md
```
Should match the execution_context reference.

Verify execution-tracker is referenced:
```bash
grep "execution-tracker" .claude/commands/agency/build.md
```
Should match.

Verify it's no longer a scaffold:
```bash
grep -c "Phase 4" .claude/commands/agency/build.md
```
Should return 0 (no more "Phase 4 will implement this" placeholders).

Verify Agent tool is in allowed-tools:
```bash
grep "Agent" .claude/commands/agency/build.md
```
Should match.

Verify it has at least 6 process steps:
```bash
grep -c "^[0-9]\+\." .claude/commands/agency/build.md
```
Should return 6.

Verify AskUserQuestion confirmation gate:
```bash
grep "AskUserQuestion" .claude/commands/agency/build.md
```
Should match.
  </verify>
  <done>/agency:build command is fully functional with 6-step process: determine phase, discover plans, confirm execution, execute waves (parallel agents with personality injection), complete phase, route to review.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to mark EXEC requirements complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update `.planning/REQUIREMENTS.md` to mark EXEC-01 through EXEC-06 as complete (change `- [ ]` to `- [x]`).

Only update the EXEC requirements — do not modify any other sections.
  </action>
  <verify>
```bash
grep -c "\- \[x\] EXEC" .planning/REQUIREMENTS.md
```
Should return 6.
  </verify>
  <done>EXEC-01 through EXEC-06 marked as complete in REQUIREMENTS.md.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.claude/commands/agency/build.md` is no longer a scaffold — has full 6-step process
- [ ] Command references all 4 skills: workflow-common, agent-registry, wave-executor, execution-tracker
- [ ] Command references state files in context section (PROJECT, ROADMAP, STATE)
- [ ] Process includes phase detection with --phase flag and auto-detect fallback
- [ ] Process includes plan discovery with validation
- [ ] Process includes pre-execution confirmation via AskUserQuestion
- [ ] Process follows wave-executor sections for parallel agent dispatch with personality injection
- [ ] Process follows execution-tracker sections for progress tracking and atomic commits
- [ ] Process handles both success and failure paths
- [ ] Process routes to /agency:review on completion
- [ ] Agent and TeamCreate are in allowed-tools
- [ ] EXEC requirements marked complete in REQUIREMENTS.md
- [ ] No placeholder references to "Phase 4" remain in build.md
</verification>

<success_criteria>
- /agency:build is a complete, executable command (not a scaffold)
- The 6-step process covers the full flow from phase detection to completion routing
- Skills are properly wired via execution_context references
- Parallel execution uses Agent tool with personality injection
- Progress tracking updates STATE.md and ROADMAP.md throughout
- Atomic git commits are created per successful plan
- User has a confirmation gate before execution starts
- Error handling stops at wave boundaries and routes to review
- Requirements tracking is updated
</success_criteria>

<output>
After completion, create `.planning/phases/04-parallel-execution/04-03-SUMMARY.md`

Then update `.planning/STATE.md`:
- Phase: 4 of 6 (planned)
- Status: Phase 4 planned — 3 plans across 2 waves
- Last Activity: Phase 4 planning (2026-03-01)
- Next Action: Run `/agency:build` to execute Phase 4: Parallel Execution

And update `.planning/ROADMAP.md` progress table:
- Phase 4: Plans = 3, Completed = 0, Status = Planned
</output>
